package io.github.edmm.plugins.cfn.util;

class AwsTypeConstants {
    private static final String DELIMITER = "::";
    private static final String AWS_PREFIX = "AWS" + DELIMITER;

    static final String AccessAnalyzer = AWS_PREFIX + "AccessAnalyzer" + DELIMITER;
    static final String ACMPCA = AWS_PREFIX + "ACMPCA" + DELIMITER;
    static final String AmazonMQ = AWS_PREFIX + "AAmazonMQ" + DELIMITER;
    static final String Amplify = AWS_PREFIX + "Amplify" + DELIMITER;
    static final String ApiGateway = AWS_PREFIX + "ApiGateway" + DELIMITER;
    static final String ApiGatewayV2 = AWS_PREFIX + "ApiGatewayV2" + DELIMITER;
    static final String AppConfig = AWS_PREFIX + "AppConfig" + DELIMITER;
    static final String ApplicationAutoScaling = AWS_PREFIX + "ApplicationAutoScaling" + DELIMITER;
    static final String AppMesh = AWS_PREFIX + "AppMesh" + DELIMITER;
    static final String AppStream = AWS_PREFIX + "AppStream" + DELIMITER;
    static final String AppSync = AWS_PREFIX + "AppSync" + DELIMITER;
    static final String Athena = AWS_PREFIX + "Athena" + DELIMITER;
    static final String AutoScalingPlans = AWS_PREFIX + "AutoScalingPlans" + DELIMITER;
    static final String AutoScaling = AWS_PREFIX + "AutoScaling" + DELIMITER;
    static final String Backup = AWS_PREFIX + "Backup" + DELIMITER;
    static final String Batch = AWS_PREFIX + "Batch" + DELIMITER;
    static final String Budgets = AWS_PREFIX + "Budgets" + DELIMITER;
    static final String Cassandra = AWS_PREFIX + "Cassandra" + DELIMITER;
    static final String CertificateManager = AWS_PREFIX + "CertificateManager" + DELIMITER;
    static final String Chatbot = AWS_PREFIX + "Chatbot" + DELIMITER;
    static final String Cloud9 = AWS_PREFIX + "Cloud9" + DELIMITER;
    static final String CloudFormation = AWS_PREFIX + "CloudFormation" + DELIMITER;
    static final String CloudFront = AWS_PREFIX + "CloudFront" + DELIMITER;
    static final String CloudTrail = AWS_PREFIX + "CloudTrail" + DELIMITER;
    static final String CodeBuild = AWS_PREFIX + "CodeBuild" + DELIMITER;
    static final String CodeCommit = AWS_PREFIX + "CodeCommit" + DELIMITER;
    static final String CodeDeploy = AWS_PREFIX + "CodeDeploy" + DELIMITER;
    static final String CodeGuruProfiler = AWS_PREFIX + "CodeGuruProfiler" + DELIMITER;
    static final String CodePipeline = AWS_PREFIX + "CodePipeline" + DELIMITER;
    static final String CodeStarConnections = AWS_PREFIX + "CodeStarConnections" + DELIMITER;
    static final String CodeStarNotifications = AWS_PREFIX + "CodeStarNotifications" + DELIMITER;
    static final String CodeStar = AWS_PREFIX + "CodeStar" + DELIMITER;
    static final String Cognito = AWS_PREFIX + "Cognito" + DELIMITER;
    static final String Config = AWS_PREFIX + "Config" + DELIMITER;
    static final String DataPipeline = AWS_PREFIX + "DataPipeline" + DELIMITER;
    static final String DAX = AWS_PREFIX + "DAX" + DELIMITER;
    static final String Detective = AWS_PREFIX + "Detective" + DELIMITER;
    static final String DirectoryService = AWS_PREFIX + "DirectoryService" + DELIMITER;
    static final String DLM = AWS_PREFIX + "DLM" + DELIMITER;
    static final String DMS = AWS_PREFIX + "DMS" + DELIMITER;
    static final String DocDB = AWS_PREFIX + "DocDB" + DELIMITER;
    static final String DynamoDB = AWS_PREFIX + "DynamoDB" + DELIMITER;
    static final String EC2 = AWS_PREFIX + "EC2" + DELIMITER;
    static final String ECR = AWS_PREFIX + "ECR" + DELIMITER;
    static final String ECS = AWS_PREFIX + "ECS" + DELIMITER;
    static final String EFS = AWS_PREFIX + "EFS" + DELIMITER;
    static final String EKS = AWS_PREFIX + "EKS" + DELIMITER;
    static final String ElastiCache = AWS_PREFIX + "ElastiCache" + DELIMITER;
    static final String ElasticBeanstalk = AWS_PREFIX + "ElasticBeanstalk" + DELIMITER;
    static final String ElasticLoadBalancing = AWS_PREFIX + "ElasticLoadBalancing" + DELIMITER;
    static final String ElasticLoadBalancingV2 = AWS_PREFIX + "ElasticLoadBalancingV2" + DELIMITER;
    static final String Elasticsearch = AWS_PREFIX + "Elasticsearch" + DELIMITER;
    static final String EMR = AWS_PREFIX + "EMR" + DELIMITER;
    static final String EventSchemas = AWS_PREFIX + "EventSchemas" + DELIMITER;
    static final String Events = AWS_PREFIX + "Events" + DELIMITER;
    static final String FMS = AWS_PREFIX + "FMS" + DELIMITER;
    static final String GameLift = AWS_PREFIX + "GameLift" + DELIMITER;
    static final String GlobalAccelerator = AWS_PREFIX + "GlobalAcceleratorType" + DELIMITER;
    static final String Glue = AWS_PREFIX + "Glue" + DELIMITER;
    static final String Greengrass = AWS_PREFIX + "Greengrass" + DELIMITER;
    static final String GroundStation = AWS_PREFIX + "GroundStation" + DELIMITER;
    static final String GuardDuty = AWS_PREFIX + "GuardDuty" + DELIMITER;
    static final String IAM = AWS_PREFIX + "IAM" + DELIMITER;
    static final String ImageBuilder = AWS_PREFIX + "ImageBuilder" + DELIMITER;
    static final String Inspector = AWS_PREFIX + "Inspector" + DELIMITER;
    static final String IoT1Click = AWS_PREFIX + "IoT1Click" + DELIMITER;
    static final String IoTAnalytics = AWS_PREFIX + "IoTAnalytics" + DELIMITER;
    static final String IoTEvents = AWS_PREFIX + "IoTEvents" + DELIMITER;
    static final String IoTThingsGraph = AWS_PREFIX + "IoTThingsGraph" + DELIMITER;
    static final String IoT = AWS_PREFIX + "IoT" + DELIMITER;
    static final String KinesisAnalytics = AWS_PREFIX + "KinesisAnalytics" + DELIMITER;
    static final String KinesisAnalyticsV2 = AWS_PREFIX + "KinesisAnalyticsV2" + DELIMITER;
    static final String KinesisFirehose = AWS_PREFIX + "KinesisFirehoseType" + DELIMITER;
    static final String Kinesis = AWS_PREFIX + "Kinesis" + DELIMITER;
    static final String KMS = AWS_PREFIX + "KMS" + DELIMITER;
    static final String LakeFormation = AWS_PREFIX + "LakeFormation" + DELIMITER;
    static final String Lambda = AWS_PREFIX + "Lambda" + DELIMITER;
    static final String Macie = AWS_PREFIX + "Macie" + DELIMITER;
    static final String ManagedBlockchain = AWS_PREFIX + "ManagedBlockchain" + DELIMITER;
    static final String MediaConvert = AWS_PREFIX + "MediaConvert" + DELIMITER;
    static final String MediaLive = AWS_PREFIX + "MediaLive" + DELIMITER;
    static final String MediaStore = AWS_PREFIX + "MediaStore" + DELIMITER;
    static final String MSK = AWS_PREFIX + "MSK" + DELIMITER;
    static final String Neptune = AWS_PREFIX + "Neptune" + DELIMITER;
    static final String NetworkManager = AWS_PREFIX + "NetworkManager" + DELIMITER;
    static final String OpsWorksCM = AWS_PREFIX + "OpsWorksCM" + DELIMITER;
    static final String OpsWork = AWS_PREFIX + "OpsWork" + DELIMITER;
    static final String PinpointEmail = AWS_PREFIX + "PinpointEmail" + DELIMITER;
    static final String Pinpoint = AWS_PREFIX + "Pinpoint" + DELIMITER;
    static final String QLDB = AWS_PREFIX + "QLDB" + DELIMITER;
    static final String RAM = AWS_PREFIX + "RAM" + DELIMITER;
    static final String RDS = AWS_PREFIX + "RDS" + DELIMITER;
    static final String Redshift = AWS_PREFIX + "Redshift" + DELIMITER;
    static final String ResourceGroups = AWS_PREFIX + "ResourceGroups" + DELIMITER;
    static final String RoboMaker = AWS_PREFIX + "RoboMaker" + DELIMITER;
    static final String Route53Resolver = AWS_PREFIX + "Route53Resolver" + DELIMITER;
    static final String Route53 = AWS_PREFIX + "Route53" + DELIMITER;
    static final String S3 = AWS_PREFIX + "S3" + DELIMITER;
    static final String SageMaker = AWS_PREFIX + "SageMaker" + DELIMITER;
    static final String SDB = AWS_PREFIX + "SDB" + DELIMITER;
    static final String SecretsManager = AWS_PREFIX + "SecretsManager" + DELIMITER;
    static final String SecurityHub = AWS_PREFIX + "SecurityHub" + DELIMITER;
    static final String ServiceCatalog = AWS_PREFIX + "ServiceCatalog" + DELIMITER;
    static final String ServiceDiscovery = AWS_PREFIX + "ServiceDiscovery" + DELIMITER;
    static final String SES = AWS_PREFIX + "SES" + DELIMITER;
    static final String SNS = AWS_PREFIX + "SNS" + DELIMITER;
    static final String SQS = AWS_PREFIX + "SQS" + DELIMITER;
    static final String SSM = AWS_PREFIX + "SSM" + DELIMITER;
    static final String StepFunctions = AWS_PREFIX + "StepFunctions" + DELIMITER;
    static final String Synthetics = AWS_PREFIX + "Synthetics" + DELIMITER;
    static final String Transfer = AWS_PREFIX + "Transfer" + DELIMITER;
    static final String WAFRegional = AWS_PREFIX + "WAFRegional" + DELIMITER;
    static final String WAF = AWS_PREFIX + "WAF" + DELIMITER;
    static final String WAFv2 = AWS_PREFIX + "WAFv2" + DELIMITER;
    static final String WorkSpaces = AWS_PREFIX + "WorkSpaces" + DELIMITER;

}
