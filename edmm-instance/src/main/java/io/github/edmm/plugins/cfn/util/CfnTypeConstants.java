package io.github.edmm.plugins.cfn.util;

import io.github.edmm.util.Constants;

class CfnTypeConstants {
    private static final String AWS_PREFIX = "AWS" + Constants.DELIMITER;

    static final String AccessAnalyzer = AWS_PREFIX + "AccessAnalyzer" + Constants.DELIMITER;
    static final String ACMPCA = AWS_PREFIX + "ACMPCA" + Constants.DELIMITER;
    static final String AmazonMQ = AWS_PREFIX + "AAmazonMQ" + Constants.DELIMITER;
    static final String Amplify = AWS_PREFIX + "Amplify" + Constants.DELIMITER;
    static final String ApiGateway = AWS_PREFIX + "ApiGateway" + Constants.DELIMITER;
    static final String ApiGatewayV2 = AWS_PREFIX + "ApiGatewayV2" + Constants.DELIMITER;
    static final String AppConfig = AWS_PREFIX + "AppConfig" + Constants.DELIMITER;
    static final String ApplicationAutoScaling = AWS_PREFIX + "ApplicationAutoScaling" + Constants.DELIMITER;
    static final String AppMesh = AWS_PREFIX + "AppMesh" + Constants.DELIMITER;
    static final String AppStream = AWS_PREFIX + "AppStream" + Constants.DELIMITER;
    static final String AppSync = AWS_PREFIX + "AppSync" + Constants.DELIMITER;
    static final String Athena = AWS_PREFIX + "Athena" + Constants.DELIMITER;
    static final String AutoScalingPlans = AWS_PREFIX + "AutoScalingPlans" + Constants.DELIMITER;
    static final String AutoScaling = AWS_PREFIX + "AutoScaling" + Constants.DELIMITER;
    static final String Backup = AWS_PREFIX + "Backup" + Constants.DELIMITER;
    static final String Batch = AWS_PREFIX + "Batch" + Constants.DELIMITER;
    static final String Budgets = AWS_PREFIX + "Budgets" + Constants.DELIMITER;
    static final String Cassandra = AWS_PREFIX + "Cassandra" + Constants.DELIMITER;
    static final String CertificateManager = AWS_PREFIX + "CertificateManager" + Constants.DELIMITER;
    static final String Chatbot = AWS_PREFIX + "Chatbot" + Constants.DELIMITER;
    static final String Cloud9 = AWS_PREFIX + "Cloud9" + Constants.DELIMITER;
    static final String CloudFormation = AWS_PREFIX + "CloudFormation" + Constants.DELIMITER;
    static final String CloudFront = AWS_PREFIX + "CloudFront" + Constants.DELIMITER;
    static final String CloudTrail = AWS_PREFIX + "CloudTrail" + Constants.DELIMITER;
    static final String CodeBuild = AWS_PREFIX + "CodeBuild" + Constants.DELIMITER;
    static final String CodeCommit = AWS_PREFIX + "CodeCommit" + Constants.DELIMITER;
    static final String CodeDeploy = AWS_PREFIX + "CodeDeploy" + Constants.DELIMITER;
    static final String CodeGuruProfiler = AWS_PREFIX + "CodeGuruProfiler" + Constants.DELIMITER;
    static final String CodePipeline = AWS_PREFIX + "CodePipeline" + Constants.DELIMITER;
    static final String CodeStarConnections = AWS_PREFIX + "CodeStarConnections" + Constants.DELIMITER;
    static final String CodeStarNotifications = AWS_PREFIX + "CodeStarNotifications" + Constants.DELIMITER;
    static final String CodeStar = AWS_PREFIX + "CodeStar" + Constants.DELIMITER;
    static final String Cognito = AWS_PREFIX + "Cognito" + Constants.DELIMITER;
    static final String Config = AWS_PREFIX + "Config" + Constants.DELIMITER;
    static final String DataPipeline = AWS_PREFIX + "DataPipeline" + Constants.DELIMITER;
    static final String DAX = AWS_PREFIX + "DAX" + Constants.DELIMITER;
    static final String Detective = AWS_PREFIX + "Detective" + Constants.DELIMITER;
    static final String DirectoryService = AWS_PREFIX + "DirectoryService" + Constants.DELIMITER;
    static final String DLM = AWS_PREFIX + "DLM" + Constants.DELIMITER;
    static final String DMS = AWS_PREFIX + "DMS" + Constants.DELIMITER;
    static final String DocDB = AWS_PREFIX + "DocDB" + Constants.DELIMITER;
    static final String DynamoDB = AWS_PREFIX + "DynamoDB" + Constants.DELIMITER;
    static final String EC2 = AWS_PREFIX + "EC2" + Constants.DELIMITER;
    static final String ECR = AWS_PREFIX + "ECR" + Constants.DELIMITER;
    static final String ECS = AWS_PREFIX + "ECS" + Constants.DELIMITER;
    static final String EFS = AWS_PREFIX + "EFS" + Constants.DELIMITER;
    static final String EKS = AWS_PREFIX + "EKS" + Constants.DELIMITER;
    static final String ElastiCache = AWS_PREFIX + "ElastiCache" + Constants.DELIMITER;
    static final String ElasticBeanstalk = AWS_PREFIX + "ElasticBeanstalk" + Constants.DELIMITER;
    static final String ElasticLoadBalancing = AWS_PREFIX + "ElasticLoadBalancing" + Constants.DELIMITER;
    static final String ElasticLoadBalancingV2 = AWS_PREFIX + "ElasticLoadBalancingV2" + Constants.DELIMITER;
    static final String Elasticsearch = AWS_PREFIX + "Elasticsearch" + Constants.DELIMITER;
    static final String EMR = AWS_PREFIX + "EMR" + Constants.DELIMITER;
    static final String EventSchemas = AWS_PREFIX + "EventSchemas" + Constants.DELIMITER;
    static final String Events = AWS_PREFIX + "Events" + Constants.DELIMITER;
    static final String FMS = AWS_PREFIX + "FMS" + Constants.DELIMITER;
    static final String GameLift = AWS_PREFIX + "GameLift" + Constants.DELIMITER;
    static final String GlobalAccelerator = AWS_PREFIX + "GlobalAcceleratorType" + Constants.DELIMITER;
    static final String Glue = AWS_PREFIX + "Glue" + Constants.DELIMITER;
    static final String Greengrass = AWS_PREFIX + "Greengrass" + Constants.DELIMITER;
    static final String GroundStation = AWS_PREFIX + "GroundStation" + Constants.DELIMITER;
    static final String GuardDuty = AWS_PREFIX + "GuardDuty" + Constants.DELIMITER;
    static final String IAM = AWS_PREFIX + "IAM" + Constants.DELIMITER;
    static final String ImageBuilder = AWS_PREFIX + "ImageBuilder" + Constants.DELIMITER;
    static final String Inspector = AWS_PREFIX + "Inspector" + Constants.DELIMITER;
    static final String IoT1Click = AWS_PREFIX + "IoT1Click" + Constants.DELIMITER;
    static final String IoTAnalytics = AWS_PREFIX + "IoTAnalytics" + Constants.DELIMITER;
    static final String IoTEvents = AWS_PREFIX + "IoTEvents" + Constants.DELIMITER;
    static final String IoTThingsGraph = AWS_PREFIX + "IoTThingsGraph" + Constants.DELIMITER;
    static final String IoT = AWS_PREFIX + "IoT" + Constants.DELIMITER;
    static final String KinesisAnalytics = AWS_PREFIX + "KinesisAnalytics" + Constants.DELIMITER;
    static final String KinesisAnalyticsV2 = AWS_PREFIX + "KinesisAnalyticsV2" + Constants.DELIMITER;
    static final String KinesisFirehose = AWS_PREFIX + "KinesisFirehoseType" + Constants.DELIMITER;
    static final String Kinesis = AWS_PREFIX + "Kinesis" + Constants.DELIMITER;
    static final String KMS = AWS_PREFIX + "KMS" + Constants.DELIMITER;
    static final String LakeFormation = AWS_PREFIX + "LakeFormation" + Constants.DELIMITER;
    static final String Lambda = AWS_PREFIX + "Lambda" + Constants.DELIMITER;
    static final String Macie = AWS_PREFIX + "Macie" + Constants.DELIMITER;
    static final String ManagedBlockchain = AWS_PREFIX + "ManagedBlockchain" + Constants.DELIMITER;
    static final String MediaConvert = AWS_PREFIX + "MediaConvert" + Constants.DELIMITER;
    static final String MediaLive = AWS_PREFIX + "MediaLive" + Constants.DELIMITER;
    static final String MediaStore = AWS_PREFIX + "MediaStore" + Constants.DELIMITER;
    static final String MSK = AWS_PREFIX + "MSK" + Constants.DELIMITER;
    static final String Neptune = AWS_PREFIX + "Neptune" + Constants.DELIMITER;
    static final String NetworkManager = AWS_PREFIX + "NetworkManager" + Constants.DELIMITER;
    static final String OpsWorksCM = AWS_PREFIX + "OpsWorksCM" + Constants.DELIMITER;
    static final String OpsWork = AWS_PREFIX + "OpsWork" + Constants.DELIMITER;
    static final String PinpointEmail = AWS_PREFIX + "PinpointEmail" + Constants.DELIMITER;
    static final String Pinpoint = AWS_PREFIX + "Pinpoint" + Constants.DELIMITER;
    static final String QLDB = AWS_PREFIX + "QLDB" + Constants.DELIMITER;
    static final String RAM = AWS_PREFIX + "RAM" + Constants.DELIMITER;
    static final String RDS = AWS_PREFIX + "RDS" + Constants.DELIMITER;
    static final String Redshift = AWS_PREFIX + "Redshift" + Constants.DELIMITER;
    static final String ResourceGroups = AWS_PREFIX + "ResourceGroups" + Constants.DELIMITER;
    static final String RoboMaker = AWS_PREFIX + "RoboMaker" + Constants.DELIMITER;
    static final String Route53Resolver = AWS_PREFIX + "Route53Resolver" + Constants.DELIMITER;
    static final String Route53 = AWS_PREFIX + "Route53" + Constants.DELIMITER;
    static final String S3 = AWS_PREFIX + "S3" + Constants.DELIMITER;
    static final String SageMaker = AWS_PREFIX + "SageMaker" + Constants.DELIMITER;
    static final String SDB = AWS_PREFIX + "SDB" + Constants.DELIMITER;
    static final String SecretsManager = AWS_PREFIX + "SecretsManager" + Constants.DELIMITER;
    static final String SecurityHub = AWS_PREFIX + "SecurityHub" + Constants.DELIMITER;
    static final String ServiceCatalog = AWS_PREFIX + "ServiceCatalog" + Constants.DELIMITER;
    static final String ServiceDiscovery = AWS_PREFIX + "ServiceDiscovery" + Constants.DELIMITER;
    static final String SES = AWS_PREFIX + "SES" + Constants.DELIMITER;
    static final String SNS = AWS_PREFIX + "SNS" + Constants.DELIMITER;
    static final String SQS = AWS_PREFIX + "SQS" + Constants.DELIMITER;
    static final String SSM = AWS_PREFIX + "SSM" + Constants.DELIMITER;
    static final String StepFunctions = AWS_PREFIX + "StepFunctions" + Constants.DELIMITER;
    static final String Synthetics = AWS_PREFIX + "Synthetics" + Constants.DELIMITER;
    static final String Transfer = AWS_PREFIX + "Transfer" + Constants.DELIMITER;
    static final String WAFRegional = AWS_PREFIX + "WAFRegional" + Constants.DELIMITER;
    static final String WAF = AWS_PREFIX + "WAF" + Constants.DELIMITER;
    static final String WAFv2 = AWS_PREFIX + "WAFv2" + Constants.DELIMITER;
    static final String WorkSpaces = AWS_PREFIX + "WorkSpaces" + Constants.DELIMITER;

}
