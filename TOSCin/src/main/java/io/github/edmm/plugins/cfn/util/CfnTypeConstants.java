package io.github.edmm.plugins.cfn.util;

import io.github.edmm.util.Constants;

public interface CfnTypeConstants {
    String AWS_PREFIX = "AWS" + Constants.DELIMITER;

    String AccessAnalyzer = AWS_PREFIX + "AccessAnalyzer" + Constants.DELIMITER;
    String ACMPCA = AWS_PREFIX + "ACMPCA" + Constants.DELIMITER;
    String AmazonMQ = AWS_PREFIX + "AAmazonMQ" + Constants.DELIMITER;
    String Amplify = AWS_PREFIX + "Amplify" + Constants.DELIMITER;
    String ApiGateway = AWS_PREFIX + "ApiGateway" + Constants.DELIMITER;
    String ApiGatewayV2 = AWS_PREFIX + "ApiGatewayV2" + Constants.DELIMITER;
    String AppConfig = AWS_PREFIX + "AppConfig" + Constants.DELIMITER;
    String ApplicationAutoScaling = AWS_PREFIX + "ApplicationAutoScaling" + Constants.DELIMITER;
    String AppMesh = AWS_PREFIX + "AppMesh" + Constants.DELIMITER;
    String AppStream = AWS_PREFIX + "AppStream" + Constants.DELIMITER;
    String AppSync = AWS_PREFIX + "AppSync" + Constants.DELIMITER;
    String Athena = AWS_PREFIX + "Athena" + Constants.DELIMITER;
    String AutoScalingPlans = AWS_PREFIX + "AutoScalingPlans" + Constants.DELIMITER;
    String AutoScaling = AWS_PREFIX + "AutoScaling" + Constants.DELIMITER;
    String Backup = AWS_PREFIX + "Backup" + Constants.DELIMITER;
    String Batch = AWS_PREFIX + "Batch" + Constants.DELIMITER;
    String Budgets = AWS_PREFIX + "Budgets" + Constants.DELIMITER;
    String Cassandra = AWS_PREFIX + "Cassandra" + Constants.DELIMITER;
    String CertificateManager = AWS_PREFIX + "CertificateManager" + Constants.DELIMITER;
    String Chatbot = AWS_PREFIX + "Chatbot" + Constants.DELIMITER;
    String Cloud9 = AWS_PREFIX + "Cloud9" + Constants.DELIMITER;
    String CloudFormation = AWS_PREFIX + "CloudFormation" + Constants.DELIMITER;
    String CloudFront = AWS_PREFIX + "CloudFront" + Constants.DELIMITER;
    String CloudTrail = AWS_PREFIX + "CloudTrail" + Constants.DELIMITER;
    String CodeBuild = AWS_PREFIX + "CodeBuild" + Constants.DELIMITER;
    String CodeCommit = AWS_PREFIX + "CodeCommit" + Constants.DELIMITER;
    String CodeDeploy = AWS_PREFIX + "CodeDeploy" + Constants.DELIMITER;
    String CodeGuruProfiler = AWS_PREFIX + "CodeGuruProfiler" + Constants.DELIMITER;
    String CodePipeline = AWS_PREFIX + "CodePipeline" + Constants.DELIMITER;
    String CodeStarConnections = AWS_PREFIX + "CodeStarConnections" + Constants.DELIMITER;
    String CodeStarNotifications = AWS_PREFIX + "CodeStarNotifications" + Constants.DELIMITER;
    String CodeStar = AWS_PREFIX + "CodeStar" + Constants.DELIMITER;
    String Cognito = AWS_PREFIX + "Cognito" + Constants.DELIMITER;
    String Config = AWS_PREFIX + "Config" + Constants.DELIMITER;
    String DataPipeline = AWS_PREFIX + "DataPipeline" + Constants.DELIMITER;
    String DAX = AWS_PREFIX + "DAX" + Constants.DELIMITER;
    String Detective = AWS_PREFIX + "Detective" + Constants.DELIMITER;
    String DirectoryService = AWS_PREFIX + "DirectoryService" + Constants.DELIMITER;
    String DLM = AWS_PREFIX + "DLM" + Constants.DELIMITER;
    String DMS = AWS_PREFIX + "DMS" + Constants.DELIMITER;
    String DocDB = AWS_PREFIX + "DocDB" + Constants.DELIMITER;
    String DynamoDB = AWS_PREFIX + "DynamoDB" + Constants.DELIMITER;
    String EC2 = AWS_PREFIX + "EC2" + Constants.DELIMITER;
    String EC2_INSTANCE = AWS_PREFIX + "EC2" + Constants.DELIMITER + "Instance";
    String ECR = AWS_PREFIX + "ECR" + Constants.DELIMITER;
    String ECS = AWS_PREFIX + "ECS" + Constants.DELIMITER;
    String EFS = AWS_PREFIX + "EFS" + Constants.DELIMITER;
    String EKS = AWS_PREFIX + "EKS" + Constants.DELIMITER;
    String ElastiCache = AWS_PREFIX + "ElastiCache" + Constants.DELIMITER;
    String ElasticBeanstalk = AWS_PREFIX + "ElasticBeanstalk" + Constants.DELIMITER;
    String ElasticLoadBalancing = AWS_PREFIX + "ElasticLoadBalancing" + Constants.DELIMITER;
    String ElasticLoadBalancingV2 = AWS_PREFIX + "ElasticLoadBalancingV2" + Constants.DELIMITER;
    String Elasticsearch = AWS_PREFIX + "Elasticsearch" + Constants.DELIMITER;
    String EMR = AWS_PREFIX + "EMR" + Constants.DELIMITER;
    String EventSchemas = AWS_PREFIX + "EventSchemas" + Constants.DELIMITER;
    String Events = AWS_PREFIX + "Events" + Constants.DELIMITER;
    String FMS = AWS_PREFIX + "FMS" + Constants.DELIMITER;
    String GameLift = AWS_PREFIX + "GameLift" + Constants.DELIMITER;
    String GlobalAccelerator = AWS_PREFIX + "GlobalAcceleratorType" + Constants.DELIMITER;
    String Glue = AWS_PREFIX + "Glue" + Constants.DELIMITER;
    String Greengrass = AWS_PREFIX + "Greengrass" + Constants.DELIMITER;
    String GroundStation = AWS_PREFIX + "GroundStation" + Constants.DELIMITER;
    String GuardDuty = AWS_PREFIX + "GuardDuty" + Constants.DELIMITER;
    String IAM = AWS_PREFIX + "IAM" + Constants.DELIMITER;
    String ImageBuilder = AWS_PREFIX + "ImageBuilder" + Constants.DELIMITER;
    String Inspector = AWS_PREFIX + "Inspector" + Constants.DELIMITER;
    String IoT1Click = AWS_PREFIX + "IoT1Click" + Constants.DELIMITER;
    String IoTAnalytics = AWS_PREFIX + "IoTAnalytics" + Constants.DELIMITER;
    String IoTEvents = AWS_PREFIX + "IoTEvents" + Constants.DELIMITER;
    String IoTThingsGraph = AWS_PREFIX + "IoTThingsGraph" + Constants.DELIMITER;
    String IoT = AWS_PREFIX + "IoT" + Constants.DELIMITER;
    String KinesisAnalytics = AWS_PREFIX + "KinesisAnalytics" + Constants.DELIMITER;
    String KinesisAnalyticsV2 = AWS_PREFIX + "KinesisAnalyticsV2" + Constants.DELIMITER;
    String KinesisFirehose = AWS_PREFIX + "KinesisFirehoseType" + Constants.DELIMITER;
    String Kinesis = AWS_PREFIX + "Kinesis" + Constants.DELIMITER;
    String KMS = AWS_PREFIX + "KMS" + Constants.DELIMITER;
    String LakeFormation = AWS_PREFIX + "LakeFormation" + Constants.DELIMITER;
    String Lambda = AWS_PREFIX + "Lambda" + Constants.DELIMITER;
    String Macie = AWS_PREFIX + "Macie" + Constants.DELIMITER;
    String ManagedBlockchain = AWS_PREFIX + "ManagedBlockchain" + Constants.DELIMITER;
    String MediaConvert = AWS_PREFIX + "MediaConvert" + Constants.DELIMITER;
    String MediaLive = AWS_PREFIX + "MediaLive" + Constants.DELIMITER;
    String MediaStore = AWS_PREFIX + "MediaStore" + Constants.DELIMITER;
    String MSK = AWS_PREFIX + "MSK" + Constants.DELIMITER;
    String Neptune = AWS_PREFIX + "Neptune" + Constants.DELIMITER;
    String NetworkManager = AWS_PREFIX + "NetworkManager" + Constants.DELIMITER;
    String OpsWorksCM = AWS_PREFIX + "OpsWorksCM" + Constants.DELIMITER;
    String OpsWork = AWS_PREFIX + "OpsWork" + Constants.DELIMITER;
    String PinpointEmail = AWS_PREFIX + "PinpointEmail" + Constants.DELIMITER;
    String Pinpoint = AWS_PREFIX + "Pinpoint" + Constants.DELIMITER;
    String QLDB = AWS_PREFIX + "QLDB" + Constants.DELIMITER;
    String RAM = AWS_PREFIX + "RAM" + Constants.DELIMITER;
    String RDS = AWS_PREFIX + "RDS" + Constants.DELIMITER;
    String Redshift = AWS_PREFIX + "Redshift" + Constants.DELIMITER;
    String ResourceGroups = AWS_PREFIX + "ResourceGroups" + Constants.DELIMITER;
    String RoboMaker = AWS_PREFIX + "RoboMaker" + Constants.DELIMITER;
    String Route53Resolver = AWS_PREFIX + "Route53Resolver" + Constants.DELIMITER;
    String Route53 = AWS_PREFIX + "Route53" + Constants.DELIMITER;
    String S3 = AWS_PREFIX + "S3" + Constants.DELIMITER;
    String SageMaker = AWS_PREFIX + "SageMaker" + Constants.DELIMITER;
    String SDB = AWS_PREFIX + "SDB" + Constants.DELIMITER;
    String SecretsManager = AWS_PREFIX + "SecretsManager" + Constants.DELIMITER;
    String SecurityHub = AWS_PREFIX + "SecurityHub" + Constants.DELIMITER;
    String ServiceCatalog = AWS_PREFIX + "ServiceCatalog" + Constants.DELIMITER;
    String ServiceDiscovery = AWS_PREFIX + "ServiceDiscovery" + Constants.DELIMITER;
    String SES = AWS_PREFIX + "SES" + Constants.DELIMITER;
    String SNS = AWS_PREFIX + "SNS" + Constants.DELIMITER;
    String SQS = AWS_PREFIX + "SQS" + Constants.DELIMITER;
    String SSM = AWS_PREFIX + "SSM" + Constants.DELIMITER;
    String StepFunctions = AWS_PREFIX + "StepFunctions" + Constants.DELIMITER;
    String Synthetics = AWS_PREFIX + "Synthetics" + Constants.DELIMITER;
    String Transfer = AWS_PREFIX + "Transfer" + Constants.DELIMITER;
    String WAFRegional = AWS_PREFIX + "WAFRegional" + Constants.DELIMITER;
    String WAF = AWS_PREFIX + "WAF" + Constants.DELIMITER;
    String WAFv2 = AWS_PREFIX + "WAFv2" + Constants.DELIMITER;
    String WorkSpaces = AWS_PREFIX + "WorkSpaces" + Constants.DELIMITER;
}
